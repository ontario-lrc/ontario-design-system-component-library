import { DateInputFieldType } from '../ontario-date-input-interface';
export declare const getVisibleDateFields: (dateOptions: Array<DateInputFieldType>) => {
	dayVisible: boolean;
	monthVisible: boolean;
	yearVisible: boolean;
};
